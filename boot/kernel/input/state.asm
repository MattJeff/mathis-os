; ============================================================================
; MathisOS - Input State Variables
; ============================================================================
; Variables d'etat pour clavier et souris
; - shift_state, ctrl_state
; - mouse_x, mouse_y, mouse_buttons
; - mouse_byte0/1/2, mouse_cycle
; ============================================================================

; TODO: Deplacer les variables ici depuis go64.asm
; Pour l'instant les variables restent dans go64.asm
; On les deplacera quand tout le code input sera migre
